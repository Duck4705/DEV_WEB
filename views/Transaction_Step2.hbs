<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Trang Thanh Toán</title>
    <link rel="stylesheet" href="/index.css">
    <link rel="stylesheet" href="/style.css">
    <link rel="stylesheet" href="/transaction_step2.css">
    <script src="https://unpkg.com/phosphor-icons"></script>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Anton&display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:ital,wght@0,100..900;1,100..900&display=swap" rel="stylesheet">
</head>
<body>
    <div class="top_bar" style="border-bottom: 1px rgba(140, 140, 140, 0.5) solid; justify-content: flex-start;">
        <div class="align_row" style="color: white;">
            <i style="font-size: 40px;" class="ph ph-video-camera"></i>
            <h1 style="font-size: 20px; margin: 0px 20px; font-weight: 600;">UIT THEATER</h1>
        </div>
    </div>
    <!-- Step Indicator -->
    <div class="step-indicator">
        <span class="step">1. THÔNG TIN KHÁCH HÀNG</span>
        <span class="step active">2. THANH TOÁN</span>
        <span class="step">3. HOÀN TẤT</span>
    </div>

    <!-- Jumbotron -->
    <div class="jumbotron">
        <!-- Left Form -->
        <div class="selection-box" style="width: 572.787px;">
            <h2>Chọn phương thức thanh toán</h2>
            <div class="payment-options">
                <form id="payment-form" class="form-action" action="/payment/transaction_step2" method="POST">
                    <input type="hidden" name="bookingId" value="{{bookingId}}">
                    <div class="payment-inner">
                        <input type="radio" id="choice1" class="payment_method" name="payment_method" value="vnpay">
                        <label for="choice1" class="align_row" style="justify-self: left;">
                            <img src="https://cdn.brandfetch.io/idV02t6WJs/theme/dark/logo.svg?c=1dxbfHSJFAPEGdCLU4o5B" alt="vnpay">
                            <span class="text">Thanh toán qua vnpay</span>
                        </label>
                        <input type="radio" id="choice2" class="payment_method" name="payment_method" value="cash">
                        <label for="choice2" class="align_row" style="justify-self: left;">                
                            <img src="https://img.icons8.com/?size=100&id=13530&format=png&color=000000" alt="cash">
                            <span class="text">Thanh toán trực tiếp</span>
                        </label>
                    </div>
                    <div class="btn-group" style="justify-content: flex-end;">
                        <button type="button" id="btn-back" class="btn btn-secondary">Quay lại</button>
                        <button type="submit" id="btn-submit" class="btn btn-primary">Thanh toán</button>
                    </div>
                </form>
            </div>
        </div>
        <!-- Right Summary -->
        <div class="summary-container" style="width: 489.113px; transform: translate(-29.6px, 0px);">
            <h2>{{rap}}</h2>
            <p><strong>Suất chiếu:</strong> {{suatchieu}}</p>
            <p><strong>Ghế:</strong> {{seatList}}</p>
            <hr>
            <p><strong>Mã đặt vé:</strong> {{bookingId}}</p>
            <p><strong>Tổng tiền:</strong> {{totalPrice}} VND</p>
        </div>
    </div>
    <div id="message-box" class="message-box hidden">
        <div class="message-content">
            <p>Vui lòng chọn phương thức thanh toán trước khi tiếp tục!</p>
            <button id="close-message" class="btn btn-primary">Đóng</button>
        </div>
    </div>
    {{#if error}}
    <div id="toast" class="alert alert-danger" style="position: fixed; top: 20px; right: 20px; z-index: 9999;">
        {{error}}
    </div>
    <script>
        setTimeout(function() {
            document.getElementById('toast').style.display = 'none';
        }, 3000);
    </script>
    {{/if}}
    <script>
        document.getElementById('payment-form').addEventListener('submit', function (event) {
            const selectedPaymentMethod = document.querySelector('input[name="payment_method"]:checked');
            if (!selectedPaymentMethod) {
                event.preventDefault(); // Prevent form submission
                    document.getElementById('message-box').classList.remove('hidden'); // Show the message box
            }
        });

        // Close the message box
        document.getElementById('close-message').addEventListener('click', function () {
            document.getElementById('message-box').classList.add('hidden'); // Hide the message box
        });

        // Handle "Quay lại" button
        document.getElementById('btn-back').addEventListener('click', function () {
            // Get bookingId from template variable
            window.location.href = '/payment/transaction_step1?bookingId={{bookingId}}';
        });
    </script>
</body>
</html>
